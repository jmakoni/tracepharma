<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="noindex,nofollow">
    <title>Link unavailable · TracePharma</title>
    <style>
        :root {
            --ink: #1c2430;
            --muted: #5b6573;
            --line: #d8dee6;
            --bg: #f4f6f8;
            --card: #ffffff;
            --danger: #9b1c1c;
        }
        * { box-sizing: border-box; }
        body {
            margin: 0;
            font-family: "Source Sans 3", "Segoe UI", sans-serif;
            color: var(--ink);
            background: var(--bg);
            line-height: 1.5;
        }
        .wrap { max-width: 520px; margin: 0 auto; padding: 2rem 1.5rem 3rem; }
        h1 { font-family: "IBM Plex Serif", Georgia, serif; font-size: 1.5rem; margin: 0 0 0.75rem; }
        .card {
            background: var(--card);
            border: 1px solid var(--line);
            border-radius: 0.5rem;
            padding: 1.25rem 1.35rem;
        }
        .meta { color: var(--muted); margin: 0; }
        .brand { display: flex; align-items: center; margin-bottom: 1.25rem; }
        .brand img { height: 32px; width: auto; }
    </style>
    <link rel="icon" href="{{ asset('images/brand/logo-mark.svg') }}" type="image/svg+xml">
</head>
<body>
<div class="wrap">
    <header class="brand">
        <img src="{{ asset('images/brand/logo.svg') }}" alt="TracePharma">
    </header>
    <div class="card">
        <h1>This acknowledgment link is no longer valid</h1>
        <p class="meta">
            The link may have expired, already been used from another copy of the email, or is otherwise invalid.
            Contact the sender if you still need to confirm receipt of the recall notice.
        </p>
    </div>
</div>
</body>
</html>
