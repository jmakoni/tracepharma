# BH7 Bug Hunt Implementation Plan

> **For agentic workers:** Use subagent-driven-development or executing-plans. Spec: `docs/superpowers/specs/2026-08-16-bh7-bug-hunt-design.md`.

**Goal:** Kill-switch Sanctum parity + aggregation FK doctor + bulk-delete hardening; then Verification/Analytics site visibility + floor mobile scan binding.

**Architecture:** Mirror BH6 inbound kill on outbound Sanctum and documents list; doctor/migrate leftover cascade FKs; SiteAccess on Verification via exception site or actor; Livewire live/blur scan on mobile receive.

**Tech Stack:** Laravel 13, Filament 5, Stancl, Pest. Prefer `DatabaseTransactions` for Admin; no `RefreshDatabase` on central.

## Global Constraints

- No branding/Stripe/AS2 inbound/SFTP legacy cleanup.
- Minimal diffs; TDD; Support least-privilege unchanged.
- Do not invent fake tenant_id scopes; DB-per-tenant stays isolation boundary.

---

### Task 1: BH7-1 Outbound Sanctum kill

**Files:** `app/Http/Controllers/Api/V1/EpcisOutboundController.php`; `app/Support/Tenancy/TenantKillSwitches.php`; `tests/Feature/Tenants/TenantKillSwitchesTest.php`

- [ ] Failing test: `outbound_epcis` killed → `POST /api/v1/epcis/outbound` 403 before persist
- [ ] `assertNotKilled(OUTBOUND_EPCIS)` at controller entry
- [ ] Tests green

### Task 2: BH7-2 Documents list inbound kill

**Files:** `app/Http/Controllers/Api/V1/EpcisDocumentsController.php`

- [ ] Failing test: `inbound_epcis` killed → `GET /api/v1/epcis/documents` 403
- [ ] Assert kill at index
- [ ] Tests green

### Task 3: BH7-3 Aggregation FK doctor

**Files:** new Artisan command e.g. `tracepharma:doctor-aggregation-link-fk`; tenant migration `database/migrations/tenant/2026_08_16_180000_preserve_retired_aggregation_links_on_event_prune.php`

- [ ] Detect tenants where `aggregation_links.established_by_event_id` still has `ON DELETE CASCADE`
- [ ] Command reports + optionally runs `tenants:migrate` for those tenants
- [ ] Test: doctor detects cascade vs dropped FK

### Task 4: BH7-4 Bulk delete partial failure

**Files:** `app/Filament/Admin/Resources/Tenants/Tables/TenantsTable.php`; `app/Actions/Tenants/DeleteTenantPair.php`; `tests/Feature/Tenants/TenantBulkDeleteTest.php`

- [ ] On per-tenant failure: stop or clearly report which deleted / which failed
- [ ] Test partial-failure path

### Task 5: BH7-5 Verification history SiteAccess

**Files:** `app/Filament/App/Resources/Verifications/VerificationResource.php`; SiteAccess helpers; tests under `tests/Feature/Vrs/`

- [ ] Site-restricted: exception at allowed sites **OR** created by current user
- [ ] Access-all: unchanged
- [ ] Tests for both personas

### Task 6: BH7-6 Analytics vrsRates alignment

**Files:** `app/Support/Dashboard/AnalyticsMetrics.php` (`vrsRates`); analytics tests

- [ ] Site-restricted rates match BH7-5 visibility
- [ ] Access-all unchanged

### Task 7: BH7-7 Floor mobile receive scan binding

**Files:** `resources/views/filament/app/resources/receiving-sessions/pages/mobile-view-receiving-session.blade.php`

- [ ] Use live/blur scan pattern matching desktop/hardware scanner flow
- [ ] Extend receive/floor tests if present

### Task 8: Close-out

- [ ] Combined regression suite
- [ ] Update `docs/roadmap-status.md` Bug-hunt #7 shipped lines

## Execution

**1. Subagent-Driven (recommended)** — fresh subagent per task  
**2. Inline Execution** — this session with checkpoints  

Which approach?
